#!/usr/bin/env python2.4

"""
Convert a Python source file into an XML representation. Uses the python DTD
generated by the "create_python_dtd" program.

"""

import sys
import pyanalyze

def main(argv):
    if len(argv) > 1:
        pyanalyze.py2xml(argv[1])
    else:
        print __doc__

main(sys.argv)
