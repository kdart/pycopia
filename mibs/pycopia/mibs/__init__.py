#!/usr/bin/python

"""
A package repository for compiled mibs. All of the files in this package are
generated by SMI.Compile module. The mib2py program is the top level script
that when run, will automatically place compiled mibs into this package
directory. 

"""

import sys
import os

# TODO get from some config.
USERMIBDIR = os.environ.get("USERMIBDIR", os.path.join("/", "var", "tmp", "mibs"))
if not os.path.isdir(USERMIBDIR):
  os.mkdir(USERMIBDIR)


# Add user generated mibs to path to pick up user generated mib files.
__path__.append(USERMIBDIR)

